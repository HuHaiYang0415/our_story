---
description: 构建发布到 demo 仓库根目录
globs: Cabinet/package.json,Cabinet/scripts/**,index.html,assets/**
alwaysApply: false
---

# 构建与发布

## 命令

```bash
cd Cabinet
npm run build:site   # vite build + copy-site.mjs
```

## 发布物（复制到上级 `demo/` 根）

- `index.html`
- `assets/`（整目录替换）
- `pages/letters/520/`（520 静态站，整目录替换）
- 根目录 `icon.png`、`manifest.webmanifest` 等随 dist 同步

`copy-site.mjs` 会先删除根目录旧的 `assets/`、`pages/`，并清理遗留目录（`20260520` 等）。

## 提交建议

- 源码改动在 `Cabinet/src/`；**线上可见变更必须包含** `build:site` 后的根目录产物。
- 不要提交 `.env`、密钥；`.cursor/` 规则可提交以便协作。
- 本地验证：`npm run dev` 测交互；发布前可用 `npm run preview` 或根目录静态服抽查 hash 路由。

## 勿做

- 不要手动只改根目录 `assets/` 而不改源码。
- 不要 `push --force` 到 main，除非明确要求。
