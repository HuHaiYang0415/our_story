# 场景素材说明

v2 主场景为 **CSS 渐变 + 内联 SVG**（见 `scroll/decor/`），不依赖本目录图片。

| 路径 | 用途 |
|------|------|
| `../boats/*.svg` | 三艘竞渡龙舟（`scroll/decor/DragonBoatSvg.tsx` 内联） |

若需增强肆幕粽形或背景，可将素材放入此目录，并在 `FestiveSplitLayout.tsx` / `ZongziMark.tsx` 中引用。
