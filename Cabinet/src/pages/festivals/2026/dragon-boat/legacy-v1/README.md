# 端午页 v1（江畔条案 · 已归档）

> 2026-06 起由 **端午长卷 v2**（`../scroll/`）替代。  
> 保留此目录供对照与回滚参考。

入口：`index.tsx`（原全屏景观 + 热区弹窗）

## 恢复 v1（仅开发）

在根目录 `index.tsx` 中改回：

```tsx
export { default } from './legacy-v1/index';
```
