# 520 陪伴互动 H5

源码目录：`Cabinet/src/pages/letters/interactive/520/`  
部署目录名：`20260520/`（与日期 2026.05.20 一致）

## 流程

开场 → 过渡 → 散步 → 出游照 → 仙女棒 → 牵手 → 夜宵 → 窗帘后礼物 → 永生花 → 520 结尾

## 本地预览

```bash
cd Cabinet
npm run dev
# 访问 http://localhost:3000/20260520/
```

或在本目录：

```bash
python -m http.server 8080
```

## 资源目录

| 目录 | 内容 |
|------|------|
| `image/` | 背景图、出游照、散步插图 |
| `bgm/` | 背景音乐（如 `粉雾海.mp3`） |

文案与资源路径在 `content.js` 的 `ASSETS` 中配置。

## 构建发布

在 `Cabinet` 下执行 `npm run build:site`，会将本目录复制到站点根 `20260520/`（构建产物，勿在仓库根手改副本）。

旧路径 `202660520/` 的跳转页由构建脚本自动生成，无需在 Git 中维护。
