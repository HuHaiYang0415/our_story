---
status: active
owner: 项目维护者
---

# Our Story · 视觉与交互设计约定

本文是当前视觉方向的基础契约；详细范围以 [`SCOPE.md`](./SCOPE.md) 为准。专题页可以保留叙事差异，但不得让共享导航、加载、错误、静音、焦点和安全区行为重新学习。

## 设计目标

- 温柔：使用纸张、木材、手写和纪念物语境，控制装饰密度。
- 私密：像两人共同保存的物件，不使用营销型指标、仪表盘和泛化 CTA。
- 手作：通过真实内容、克制的不规则边缘、纸张/木纹材料和有来源的字形建立质感，不依赖滤镜堆叠。

## 材料与色彩

基础材料是暖白纸张、深浅木色、邮票/信笺和少量朱印或节庆强调色。当前全局 CSS 令牌包括：

| 令牌 | 当前值 | 用途 |
|------|--------|------|
| `--color-brand-bg` | `#FDFBF7` | 日间主背景 |
| `--color-brand-text` | `#2D241E` | 主文字 |
| `--color-wood-dark` | `#5A3E23` | 深木色 |
| `--color-wood-mid` | `#6D4C2B` | 中木色 |
| `--color-wood-light` | `#8C6239` | 浅木色/强调 |
| `--color-wood-shelf` | `#4c3217` | 层板与暗部 |

页面可以使用专题色，但应说明其材料来源和使用范围；不要把紫色渐变、玻璃模糊或透明叠层当作默认品牌语言。

## 三层视觉令牌

令牌分成“材料基础 → 语义组件 → 专题色”三层。页面只在确有叙事差异时使用专题令牌；共享返回、加载、错误、静音和焦点状态只使用基础/语义层。

### 材料基础层

| CSS 令牌 | 默认值 | 材料含义 |
| --- | --- | --- |
| `--color-brand-bg` | `#FDFBF7` | 暖白纸张底 |
| `--color-surface-paper` | `#FFFDFB` | 干净纸面 |
| `--color-surface-paper-soft` | `#FCFAF2` | 旧纸/档案底 |
| `--color-wood-dark` / `mid` / `light` | `#5A3E23` / `#6D4C2B` / `#8C6239` | 木柜边框、层板和金棕强调 |
| `--color-wood-shelf` | `#4C3217` | 木层板暗部 |
| `--color-line-paper` | `#E5DACE` | 纸张边线与分隔 |
| `--font-serif` / `--font-sans` / `--font-hand` | Playfair/系统中文/手写字形 | 阅读、界面、短注记 |

### 语义组件层

| 语义 | 统一实现 | 使用规则 |
| --- | --- | --- |
| 页面表面/正文 | `--story-surface`、`--story-ink` | 跟随日夜主题，不在页面重复写基础色 |
| 辅助文字/边线 | `--story-ink-muted`、`--story-line` | 辅助文字仍需保持可读对比度 |
| 返回按钮 | `.story-action-button` / `StoryBackButton` | 44px 高度、左箭头、动作文字 |
| 静音按钮 | `.story-icon-button` / `StoryMuteButton` | 图标、`aria-pressed`、状态化名称统一 |
| 加载/错误动作 | `StoryInlineButton` + `FestiveLoadScreen` | 重试和返回顺序一致，错误保留恢复路径 |
| 动效节奏 | `--story-duration-*`、`--story-ease-out` | 默认使用 160/280/480ms；不用弹跳/回弹作为共享节奏 |

### 专题层

端午使用 `emerald`，儿童节使用 `amber`，相恋时光使用 `rose`，信箱/展柜使用 `wood`。专题可保留自己的纸张、河流、儿童房和私人纪念物图形，但不得重写共享控件的焦点、触控热区、返回和静音语义。

## 字体角色

- 正文与长段落：`--font-serif`，优先保证中文和小屏阅读。
- 展柜 UI 与辅助信息：`--font-sans`，避免全大写宽字距成为默认层级。
- 手写注记与节庆题签：`--font-hand`，只用于短文本，不承载关键说明。

当前字体定义位于 `Cabinet/src/index.css`。新增字形、扫描笔迹或私人手写素材时，记录来源、授权和可读性评估。

## 布局与组件

- 主页面使用统一 Viewport/Stage 壳，保留 `dvh` 与 safe area 处理。
- 返回、加载、错误、静音和焦点状态采用稳定的语义按钮和可见反馈。
- 主要触控区域目标为 44px；正文缩放至 200% 仍应可阅读和返回。
- 圆角、阴影和纹理用于表达纸张/木材边界，不用于把每个区域包装成漂浮玻璃卡片。

## 动效与声音

- 动效表达翻页、展开、漂浮或纸张移动等物理隐喻，优先使用克制的 ease-out 节奏。
- `prefers-reduced-motion` 下保留状态变化，停用无限粒子、弹跳和非必要位移。
- 音频必须有清晰的静音入口；页面离开时停止并释放实例，不把大音频作为首屏阻塞条件。
- 共享动效节奏集中在 `Cabinet/src/index.css` 的 `--story-duration-*` 和 `--story-ease-out`；状态变化优先使用透明度、颜色或短距离位移，不使用通用 `bounce`、`spin` 作为默认反馈。

## 去模板化约束

- 界面文字优先使用中文语义和真实纪念物语境；英文仅作为内容的一部分，不用全大写宽字距制造“高级感”。
- 模糊只用于明确的遮罩或照片景深，不作为每个卡片的通用表面；共享按钮使用纸张/木材边界和一层轻阴影。
- 紫色渐变、玻璃卡片、弹跳和旋转不再作为共享语言；端午的绿、儿童节的暖色和相册的季节氛围属于专题例外，并应服务于叙事。
- 新增图片、音频、字形或私人扫描件必须先登记来源、授权、压缩版本和隐私/可读性评估，记录格式见 [`Cabinet/docs/MEDIA-PROVENANCE.md`](Cabinet/docs/MEDIA-PROVENANCE.md)。

## 专题边界

儿童节、端午和七夕可以拥有自己的颜色、图形和叙事。专题素材和状态留在专题目录，不提升到共享层；共享层只承载稳定的布局、主题、加载和导航契约。

七夕后续设计与开发仅面向 v2。v1 停止迭代，仅保留历史源码与现有发布产物；本次不修改 v1，也不新增继续使用 v1 的产品入口。已发布状态仍以 `SCOPE.md` 为准，后续不使用 v1 的产品决定不等于已经下线旧版或发布 v2。

2026-09-15 确认的 v2 布局契约：整个体验保持一页单屏，页面根容器、外层页面槽和餐桌舞台均不可横向或纵向滚动；上菜、甜品盘和来信在同一可视区域内切换，不向下追加章节。沿用 Viewport/Stage 的动态视口与 safe area 处理，按宽度和可用高度共同适配手机、平板、桌面及横屏短屏。优先减少装饰、缩短留白和调整构图，不整体缩小正文或裁切功能内容来凑单屏。

长信仅允许信纸内部纵向滚动，信尾入口位于同一阅读区域；外层舞台保持静止。返回始终可见，播放期间暂停/继续及直接读信可达；正文放大至 200%、键盘与触控操作、加载/错误状态和 `prefers-reduced-motion` 均遵循同一布局约束。v1 的旧短屏滚动实现不再作为 v2 设计规则；这些是待实施的 v2 要求，不代表现有样稿已经符合。七夕专题及其本地修订文档明确排除于本次提交／发布，原工作树保留供后续使用。

## 评审清单

新增或改造页面前检查：

1. 是否说明了材料、色彩和字形来源？
2. 是否沿用统一返回、加载、错误、静音、焦点和安全区行为？
3. 是否覆盖键盘、触控、窄屏、200% 缩放和 reduced-motion？
4. 是否避免通用字体组合、全大写宽字距、装饰性玻璃模糊、紫色渐变和弹跳？
5. 是否把专题状态、素材和音频留在专题边界内？

后续视觉令牌收敛和机械检测结果按任务清单推进，不把目标态检查项误写成当前已完成事实。
