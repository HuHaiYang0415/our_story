---
status: active
authority: current-scope
owner: 项目维护者
last_reviewed: 2026-09-18
release_profile: approved-without-qixi-and-local-tests
---

# Our Story 当前范围

本文件是当前范围、基线与发布许可的唯一权威。产品意图、设计提案和历史验收不自动构成发布授权。

## 2026-09-18 本次明确授权

- 用户要求重新构建、提交并推送七夕页及本地测试内容、按钮以外的已有改动，包括展柜、信箱与 520、相册、相恋时光、节日档案馆、儿童节、端午及必要共享基础与文档。
- 用户随后明确批准公开五组相册照片及已提供地址。分组为迪士尼 72、告白 1、节日 2、灵隐寺 1、日常 11，共 87 张；封面、三组日期与地址按用户确认数据，不补造缺失日期。
- 相册首页一册一封面；首次点击居中到前景并上提 16px，再次独立点击进入。册内直接展示当前原图，底部横向压缩缩略图带；不设进入按钮、小记、纯色相纸垫底或“本地素材 · 未发布”提示。
- 相纸页日夜暂统一浅色叶影，不改其他页面主题。地图只保留全国／城市两层、连续周边与地理锚点附近封面选择，不设固定右栏、第三层放大或未定位入口。地址可以展示，但地图数据只支持城市代表点，不声称精确住址坐标，也不向第三方发送地址。
- 原素材只读。经本次批准的原图字节复制到站点根 `gallery/originals/`，仅保留一套跟踪文件；Vite 构建复制到忽略 Git 的 `Cabinet/dist/`。封面、地图与条带只用 400px / quality 60 缩略图；仅册内当前大图请求原图，不预取整册。

## 发布边界与版本

- 历史线上基线为 `8eb34f3`（撤回未完成七夕页面发布）。本次提交是上述内容的新发布候选，推送成功后以 Git 提交与远端实际状态记录新基线。
- 发布候选使用隔离工作树构建，移除七夕专题源码／路由／懒加载入口及本地测试入口、日期模拟和季节切换按钮；不提交七夕草稿、个人白名单、机器路径、模拟状态、截图缓存或代理工具缓存。
- 原工作树的七夕与 DEV 预览仍可保留供本地工作，不构成本次发布。先前任务9的七夕 released 与根目录构建记录是本地历史，不能覆盖本次明确排除。
- `npm run verify` 验收候选源码；`npm run build:site` 必须同时核对本文件授权和无七夕／测试产物门禁后，才同步 `index.html`、`assets/`、`pages/` 等生成输出。不得手工替代源码修改发布产物。
- 原图约 753 MiB，最大单文件约 22.4 MiB；不得为规避体积而把所有大图改成首屏加载。原始私人文件与地址本次有明确公开许可，后续新增内容仍需另行确认。

## 不属于范围

- 不发布七夕页，不把日期或本地预览自动变成发布许可。
- 不接入私密访问后端、数据库、对象存储或上传／编辑能力，不将平台目标态自动视为实施许可。
- 不顺手重设计其他页面；后续图片先给提示词，由用户生成并提供。

## 验收

- 相册居中选中、再次进入、横向条带、当前大图原图与小图压缩均有浏览器证据；桌面、手机、短横屏保持单屏无纵向滚动。
- 发布产物没有七夕专题 chunk、测试按钮或本机路径；五组公开内容与 87 张原图／缩略图对应，原图 SHA-256 与用户源文件一致。
- 候选通过 `npm run verify` 与发布门禁，再以非强制提交、推送记录结果；保留本地未发布工作。

## 文档

- [产品](PRODUCT.md)、[设计](DESIGN.md)、[架构](docs/ARCHITECTURE.md)、[协作规则](AGENTS.md)
- [相册 PRD 与对话补充](changes/gallery-album/PRD-2026-09-18.md)
- [资源加载](Cabinet/docs/RESOURCE-LOADING-GUIDELINES.md)、[内容契约](docs/CONTENT-CONTRACT.md)
- [平台目标态提案](changes/platform-foundation/proposal.md)、[执行任务](changes/platform-foundation/tasks.md)
